---
language: python
services: docker

env:
  global:
    - ROLE_NAME: pms

install:
  # Install test dependencies.
  - pip install molecule docker

before_script:
  # Use actual Ansible Galaxy role name for the project directory.
  - cd ../
  - mv ansible-role-$ROLE_NAME jcalavia.$ROLE_NAME
  - cd jcalavia.$ROLE_NAME

script:
  # Run tests.
  - molecule test

notifications:
  webhooks: 
  if: branch = master 
  urls:
    - https://galaxy.ansible.com/api/v1/notifications/

  slack: jcalavia-org:UaZ60aCeDgo6ISl60fd5vgXf